[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pfa-ir-schema"
version = "0.1.0"
description = "IR schema data models for personal finance analysis (bank statement intermediate representation)"
requires-python = ">=3.12"
dependencies = []

[project.urls]
Homepage = "https://github.com/shaowen310/personal-finance-cli"

[tool.hatch.build.targets.wheel]
packages = ["src/pfa_ir_schema"]
